services:
  - type: web
    name: targa-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    workingDirectory: my-backend
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_VERSION
        value: 20
